; {{ ansible_managed }}

; FQDN
127.0.1.1 {{  hostvars[inventory_hostname]['ansible_hostname'] }}.{{localdomain}}  {{hostvars[inventory_hostname]['ansible_hostname']}}

; Loopback
127.0.0.1 localhost
